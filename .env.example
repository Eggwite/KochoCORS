# Example configuration file for the proxy server
# Copy this to .env and modify as needed

# Port to listen on (default: 3000)
PORT=3000

# Authentication key for proxy requests (optional)
# AUTH_KEY=your-secret-key

# Comma-separated list of allowed domains (optional, allows all if not set)
# ALLOWED_DOMAINS=example.com,api.example.com,localhost

# Rate limit per minute (0 to disable, default: 0)
# RATE_LIMIT=60

# Enable debug logging (default: false)
# DEBUG=true

# Skip TLS certificate verification for HTTPS targets (default: false)
# INSECURE_TLS=true

# Default origin for CORS headers (default: *)
# DEFAULT_ORIGIN=https://yourdomain.com
